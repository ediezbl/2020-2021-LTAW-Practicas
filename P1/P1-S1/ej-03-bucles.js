// Ejemplo de utilización de bucles 

// Definiendo una constante, en este caso es el número de 
// mensajes que vamos a imprimir por pantalla 
const N = 10;

// Bucle que va a permitir imprimir los 10 mensajes 
for (i= 0; i < N; i++) {
    console.log("Mensaje ", + i);
}